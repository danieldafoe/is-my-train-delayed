import { FormatRefreshTimePipe } from './format-refresh-time.pipe';

describe('FormatRefreshTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRefreshTimePipe();
    expect(pipe).toBeTruthy();
  });
});
